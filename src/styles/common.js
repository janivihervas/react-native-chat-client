import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  fullSize: {
    flex: 1
  },
  padding: {
    paddingLeft: 15,
    paddingRight: 15
  }
});

export const colors = {
  blue: '#42BADC',
  grey: '#DFDFDF',
  black: '#000000'
};

export default styles;
